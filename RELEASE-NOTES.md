# 发布说明 · 智序（公众号推文助手）桌面版

> 版本 0.1.0 · 2026-09-09 · 正式首版发布

## 这是什么

智序是一个跑在 Windows 电脑上的本地双栏公众号写作工作台：**左侧与 AI 对话生成推文，右侧 375px 手机壳实时预览排版成品**。AI 直连 DeepSeek API（流式），知识语料与排版引擎全部内嵌，作品与数据保存在本机 `文档/wechat-mp-workspace/`。

本版即 GitHub 仓库 `wechat-mp` 的 main 分支（旧版 DSH 公众号插件预设已迁移至 dsh 分支保留）。

## 主要功能

1. AI 对话创作：像聊天一样提需求，AI 先澄清再成稿；支持多轮修改、反问取消、闲聊；内容类型与风格由 AI 按需求自决。
2. 公众号排版引擎：三层知识语料（文本/视觉/插图/其它，149 条目）内嵌；小标题、提示气泡、分隔线、照片位、横幅插画等成套组件统一 375px 手机版式渲染；生成后自动质检（可修项自动回喂重写，最多两轮）。
3. 个人素材库 + 素材工坊：插画素材分类制作入库（气泡角饰/分割线/装饰/横幅/标题图/插图/相框），创作时自动检索复用库内素材（`[[asset]]` 引用、固化快照），替换源带版本与影响扫描。
4. 文档库：每篇成稿自动保存为文档，打开回到源会话续改，就地刷新不产生两版。
5. 发布（离线）：一键导出 375 宽 2x 高清长图 + 按屏分页 PNG（或导出 HTML），自行上传公众号后台发布；不代发、不依赖微信接口白名单。
6. 多会话管理：会话侧栏新建/切换/删除，自动存档重启不丢。
7. 使用手册：应用内「使用手册」按钮（或安装目录 `使用手册.html`），面向零基础用户的图文教程。
8. 密钥安全：DeepSeek API Key 仅存本机（设置面板或环境变量），不入库、不上传、无遥测。

## 安装包

- 文件名：`智序_0.1.0_x64-setup.exe`（NSIS）
- 系统要求：Windows 10/11（64 位）；个人开发者未签名，SmartScreen 提示时选「更多信息 → 仍要运行」
- 数据位置：`文档/wechat-mp-workspace/`（会话/文档/素材/导出/设置），卸载不删除

## 验证情况

- cargo 单元测试 57 项通过（另 4 项 live 冒烟）；E2E S1-S14 全绿（playwright，截图见 docs/artifacts）；compose 引擎样例检查通过；真实模型 live-conformance A/B/C 合规闸门通过
- key 泄露全量扫描（工作区 + 全部历史提交）零命中
- 安装包含《使用手册》，安装闭环 + 启动冒烟验证通过

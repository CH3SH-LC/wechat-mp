# 迁移说明 —— 第 13 轮知识库三层重组（test/knowledge 镜像）

> 本目录 `test/knowledge/` 是**重组后的知识库镜像**（模拟态）：真实知识库 `skills/wechat-mp-bootstrap/knowledge/` **未动**，转正待整体确认。
> 重组方案（用户 2026-08-29 确认）：三层结构——L1 大方面 = 文本/视觉/插图/其它；L2 细化 = 内容类型/文案/合规、模块/风格、图片/视频、封面/可读性；L3 每个文件 = 细化方向上的一个点。发布/复盘等运营内容不放入本知识库。

## 一、文件归位映射（真实库 → 镜像）

| 真实库（旧） | 镜像（新） | 处理 |
|---|---|---|
| `type/`（14） | `文本/内容类型/` | 原样复制 |
| `copy/`（25）+ test 草稿 copy-subheading.md | `文本/文案/`（26） | 复制 + 并入新草稿 |
| `compliance/`（6） | `文本/合规/` | 原样复制 |
| `module/`（44） | `视觉/模块/`（46） | 7 个同名条目用 test 八节草稿版取代真实旧版（★），+module-subheading、module-inline-deco 两个新草稿 |
| `style/`（29） | `视觉/风格/` | 原样复制 |
| `image/`（4） | `插图/图片/`（5） | 原样复制 + 定位微调（链条化）+ 新增 img-insert.md（插入环） |
| — | `插图/视频/`（3） | 视频为新内容：提纲经用户确认后落盘 video-prepare.md / video-insert.md / 00-索引.md |
| `cover/`（5） | `其它/封面/` | 原样复制 |
| `readable/`（5） | `其它/可读性/` | 原样复制 |

**★ 7 个以草稿版取代**：module-badge / module-band / module-bubble / module-card / module-divider / module-list / module-steps（第 9-10 轮类型×风格八节结构版）。
**镜像根级**：新 00-GUIDE.md（三层路由）+ design-logic-components.md（设计世界观，全库先读，位置不变）+ 本迁移说明 + 交叉更新清单.md（开发清单，转正后不再属于知识库）。
**退役**：真实库根级 index-type/copy/compliance/module/style/image/cover/readable.md → 各自并入新方向目录的 00-索引.md；test 根级旧草稿 index-copy.md / index-module.md 已删除（内容并入新 00-索引）。

## 二、排除内容清单（移出 knowledge/，转正时另建 ops-knowledge/）

选题 topic（5）、定位 positioning（4）、发布 publish（5）、增长 growth（4）、复盘 review（5）、历史归档 legacy（18）——共 **41 文件**。
- 转正动作：真实库 `knowledge/{topic,positioning,publish,growth,review,legacy}/` → `ops-knowledge/`（内容不动，仅搬迁 + 归档指针说明）。
- 真实库 00-GUIDE 中被排除内容的引用行（如"平台事实见 legacy/09"）在转正时改为指向 ops-knowledge/。

## 三、插图维度改动细目

1. img-sources / img-copyright / img-processing / img-alternatives：头部定位补"图片插入链条第 N 环"一句；尾部交叉行从旧维度名改为新方向路径（如"合规维度"→ 文本/合规 的 comp-copyright.md；"legacy/05"引用删除或改指 module-image.md）。
2. **img-insert.md（新点）**：上传通道 / 版位选型 / 图注 / 防翻车五查——插入前最后一公里检查，模板代码不重复 module-image.md。
3. **视频方向（已完成，提纲 2026-08-29 确认）**：video-prepare.md（素材制作与上传）+ video-insert.md（插入路径与卡片配合）+ 00-索引.md；组件卡片模板在 module-media.md，两者分工：插图/视频 管素材与操作，视觉/模块 管排版模板。

## 四、转正执行顺序（用户确认后）

1. 把镜像新树整体落入真实知识库（git mv 真实库旧维度目录 → 按映射删除 7 个旧组件条目等）；
2. 排除 41 文件移入 `ops-knowledge/`（新目录在 `skills/wechat-mp-bootstrap/` 下），真实库内留归档指针；
3. 真实库 00-GUIDE.md 替换为本镜像 00-GUIDE.md；各方向 00-索引 落位；
4. 同步更新引用方：SKILL.md「知识库（先读再写）」节路径描述、`agent.cordis.yml` persona 的知识调用指南（旧 legacy 编号引用 07-design-text 等已过时，一并改三层路径）、dev/scripts/scan-kb-v10.mjs 的目录扫描假设（13 维度 → 三层）、REQUIREMENTS/test/README 等；
5. 重跑全库扫描 + 人工抽查引用。

## 五、验证状态（镜像自检）

- [x] 归位完整性：8 个真实维度 100% 落位（type14/copy25/compliance6/module37+9/style29/image4/cover5/readable5），无缺无重；
- [x] 排除维度文件零进入镜像；
- [x] 复制文件与真实库源逐字节一致（哈希比对）；
- [x] 9 个 ★/新条目确为 test 草稿版；
- [x] 索引 ↔ 文件互检（每个 00-索引 引用的文件存在且同目录无遗漏）；
- [x] 视频方向（video-prepare / video-insert + 00-索引）提纲用户确认后落盘 插图/视频；
- [ ] 真实库未动（对照 git status 仅 test/ 与文档变化）。
